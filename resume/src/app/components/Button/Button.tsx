"use client";
import React, { useState } from "react";

const Button = () => {
  return (
    <div>
      <button className="bg-gradient-to-r from-purple-400 to-pink-500 text-white font-bold py-2 px-4 rounded hover:from-pink-500 hover:to-yellow-500">
        Next
      </button>
    </div>
  );
};

export default Button;
